<Window x:Class="busstops.Window4"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:busstops"
        mc:Ignorable="d"
        Title="Window4" Height="155.085" Width="347.448">
    <Grid Margin="0,10,10,4" HorizontalAlignment="Right" Width="319">
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="25" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="3*"/>
            <RowDefinition Height="13*"/>
        </Grid.RowDefinitions>

        <TextBlock Margin="46,0,46,24" Width="227" HorizontalAlignment="Center" TextAlignment="Center" Grid.RowSpan="2">Тип Маршрута</TextBlock>
        <ComboBox x:Name="triptypecombobox" HorizontalAlignment="Left" VerticalAlignment="Top" Width="299" Grid.Row="1" Grid.RowSpan="2" Margin="10,1,0,0"/>

        <WrapPanel Grid.Row="4" Margin="62,8,79,3">
            <Button IsCancel="True" MinWidth="60" Content="Отмена" Width="100"/>
            <Button IsDefault="True" Click="Accept_Click" MinWidth="60" Margin="5,0,0,0" Content="OK" Width="71"/>
        </WrapPanel>
    </Grid>
</Window>
